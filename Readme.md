## Rentalcars.com JavaScript API
To get started you'll need to rename **config/rentalcars-example.js** to **config/rentalcars.js** and enter your Rentalcars credentials

Then run ``npm install`` and ``npm start```
